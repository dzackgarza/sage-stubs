from collections.abc import Iterable

from sage.categories.category import Category
from sage.categories.cartesian_product import CartesianProductFunctor
from sage.misc.call import AttrCallObject
from sage.structure.parent import Parent
from sage.structure.unique_representation import UniqueRepresentation

class CartesianProduct(UniqueRepresentation, Parent):
    def __init__(
        self, sets: Iterable[Parent], category: Category, flatten: bool = ...
    ) -> None: ...
    def __contains__(self, x: object) -> bool: ...
    def cartesian_factors(self) -> tuple[Parent, ...]: ...
    def cartesian_projection(self, i: int | str) -> AttrCallObject: ...
    def construction(self) -> tuple[CartesianProductFunctor, tuple[Parent, ...]]: ...
