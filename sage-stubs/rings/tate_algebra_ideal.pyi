from sage.rings.ideal import Ideal_generic
from sage.rings.tate_algebra_element import TateAlgebraElement

class TateAlgebraIdeal(Ideal_generic):
    def groebner_basis(self, prec: int | None = None, algorithm: str = "VaPoTe", **options: object) -> list[TateAlgebraElement]: ...
    def is_saturated(self) -> bool: ...
    def saturate(self) -> TateAlgebraIdeal: ...
