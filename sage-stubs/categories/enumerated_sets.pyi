from collections.abc import Callable, Iterator

from sage.structure.element import Element

class EnumeratedSets:
    class ElementMethods:
        ...
    class ParentMethods:
        def __iter__(self) -> Iterator[Element]: ...
        def __getitem__(self, i: int | slice) -> Element | list[Element]: ...
        def is_empty(self) -> bool: ...
        def random_element(self) -> Element: ...
        def map(self, f: Callable[[Element], Element], name: str | None = None, *, is_injective: bool = True) -> "EnumeratedSets": ...
