from collections.abc import Callable

from sage.structure.parent import Parent

class FiniteLatticePosets:
    class ParentMethods:
        def join_irreducibles(self) -> list[object]: ...
        def join_irreducibles_poset(self) -> Parent: ...
        def meet_irreducibles(self) -> list[object]: ...
        def meet_irreducibles_poset(self) -> Parent: ...
        def irreducibles_poset(self) -> Parent: ...
        def is_lattice_morphism(self, f: Callable[[object], object], codomain: Parent) -> bool: ...
