# Stubs for sage.misc.defaults
var_name: str

def variable_names(n: int, name: str | None = ...) -> tuple[str, ...]: ...
def latex_variable_names(n: int, name: str | None = ...) -> tuple[str, ...]: ...
def set_default_variable_name(name: str, separator: str = ...) -> None: ...

series_prec: int

def series_precision() -> int: ...
def set_series_precision(prec: int) -> None: ...
