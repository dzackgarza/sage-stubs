from collections.abc import Iterator
from sage.matrix.matrix import Matrix
from sage.structure.element import Element

def row_iterator(A: Matrix) -> Iterator[object]: ...
def prm_mul(p1: dict[int, Element], p2: dict[int, Element], mask_free: int, prec: int | None) -> dict[int, Element]: ...
def permanental_minor_polynomial(A: Matrix, permanent_only: bool = False, var: str = 't', prec: int | None = None) -> Element: ...
