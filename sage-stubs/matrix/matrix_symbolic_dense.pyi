from collections.abc import Mapping, Sequence
from typing import Literal, Self, overload

from sage.matrix.matrix import Matrix
from sage.matrix.matrix_generic_dense import Matrix_generic_dense
from sage.modules.free_module_element import FreeModuleElement
from sage.rings.integer import Integer
from sage.rings.polynomial.polynomial_element import Polynomial
from sage.structure.factorization import Factorization
from sage.symbolic.expression import (
    Expression,
    SymbolicInput,
    SymbolicSubstitution,
)


type SymbolicEigenvectorData = tuple[
    Expression,
    list[FreeModuleElement[Expression]],
    Integer,
]


class Matrix_symbolic_dense(Matrix_generic_dense[Expression]):
    def echelonize(self, **kwds: object) -> None: ...
    def echelon_form(self, **kwds: object) -> Self: ...
    def pivots(self) -> tuple[int, ...]: ...
    def rank(self) -> int: ...
    def determinant(self, algorithm: str | None = ...) -> Expression: ...
    det = determinant
    def trace(self) -> Expression: ...
    def eigenvalues(self, extend: bool = ...) -> list[Expression]: ...
    def eigenvectors_left(
        self,
        other: Matrix_symbolic_dense | None = ...,
    ) -> list[SymbolicEigenvectorData]: ...
    def eigenvectors_right(
        self,
        other: Matrix_symbolic_dense | None = ...,
    ) -> list[SymbolicEigenvectorData]: ...
    def exp(self) -> Self: ...

    def charpoly(
        self,
        var: str = ...,
        algorithm: str | None = ...,
    ) -> Polynomial: ...
    characteristic_polynomial = charpoly
    def minpoly(self, var: str = ...) -> Polynomial: ...
    minimal_polynomial = minpoly
    def fcp(self, var: str = ...) -> Factorization: ...

    @overload
    def jordan_form(
        self,
        subdivide: bool = ...,
        transformation: Literal[False] = ...,
    ) -> Self: ...
    @overload
    def jordan_form(
        self,
        subdivide: bool,
        transformation: Literal[True],
    ) -> tuple[Self, Self]: ...

    def simplify(self) -> Self: ...
    def simplify_trig(self) -> Self: ...
    def simplify_rational(self) -> Self: ...
    def simplify_full(self) -> Self: ...
    def canonicalize_radical(self) -> Self: ...
    def factor(self) -> Self: ...
    def expand(self) -> Self: ...
    def conjugate(self) -> Self: ...
    def real(self) -> Self: ...
    def imag(self) -> Self: ...

    def variables(self) -> tuple[Expression, ...]: ...
    def arguments(self) -> tuple[Expression, ...]: ...
    def number_of_arguments(self) -> int: ...
    def __call__(
        self,
        *args: SymbolicSubstitution,
        **kwargs: SymbolicInput,
    ) -> Self: ...
    def function(
        self,
        *args: Expression | Sequence[Expression],
    ) -> Matrix[Expression]: ...
    def subs(
        self,
        *args: SymbolicSubstitution | Mapping[Expression, SymbolicInput],
        **kwds: SymbolicInput,
    ) -> Self: ...
    substitute = subs
    def derivative(
        self,
        *args: Expression | int | Integer,
    ) -> Self: ...
    diff = derivative
    def integral(
        self,
        *args: Expression | int | Integer,
    ) -> Self: ...
    integrate = integral
    def numerical_approx(
        self,
        prec: int | Integer | None = ...,
        digits: int | Integer | None = ...,
        algorithm: str | None = ...,
    ) -> Matrix: ...
    n = numerical_approx
