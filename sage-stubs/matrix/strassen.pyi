from collections.abc import Iterable, Iterator
from typing import TypeVar

from sage.matrix.matrix_window import MatrixWindow
from sage.rings.ring import Ring
from sage.structure.element import RingElement

_Scalar = TypeVar("_Scalar", bound=RingElement, default=RingElement)

def strassen_window_multiply(
    C: MatrixWindow[_Scalar],
    A: MatrixWindow[_Scalar],
    B: MatrixWindow[_Scalar],
    cutoff: int,
) -> None: ...
def strassen_echelon(A: MatrixWindow[_Scalar], cutoff: int) -> None: ...

class int_range:
    def __init__(
        self,
        indices: int | Iterable[int] | None = ...,
        range: int | None = ...,
    ) -> None: ...
    def __repr__(self) -> str: ...
    def intervals(self) -> list[tuple[int, int]]: ...
    def to_list(self) -> list[int]: ...
    def __iter__(self) -> Iterator[tuple[int, int]]: ...
    def __len__(self) -> int: ...
    def __add__(self, right: int_range) -> int_range: ...
    def __sub__(self, right: int_range) -> int_range: ...
    def __mul__(self, right: int_range) -> int_range: ...

def test(n: int, m: int, R: Ring, c: int = ...) -> bool: ...
