# Phase 04 — Number-Theoretic Rings

**Tier:** 1
**Status:** ✅ Complete
**Depends on:** Phase 01
**Unblocks:** Phase 13, 14, 17

## Goal

Cover `sage.rings.number_field/`, `sage.rings.padics/`,
`sage.rings.finite_rings/`, `sage.rings.function_field/`,
`sage.rings.valuation/`, `sage.rings.asymptotic/`, `sage.rings.semirings/`,
`sage.rings.bernmm/`. Several have a thin existing stub footprint that must
be audited and expanded, not rewritten.

## Tasks

| Task | Subtree / Group | Files | Depends | Status | Notes |
|------|-----------------|-------|---------|--------|-------|
| T04.1 | **Finite rings: element types** — `element_base`, `element_givaro`, `element_ntl_gf2e`, `element_pari_ffelt`, `integer_mod`. | 5 | — | ✅ Done | element_base: FiniteRingElement (4) + FinitePolyExtElement (25) + Cache_base; element_givaro: FiniteField_givaroElement (35) + Cache_givaro (19) + iterator; element_ntl_gf2e / element_pari_ffelt REWRITTEN — CI population had replaced them with stubgen slop (`_SageObject`, `builtins.object`); integer_mod: IntegerMod_abstract (48) + gmp/int/int64 (30 each) + 4 hom classes + NativeIntStruct + Mod/IntegerMod/lucas/square_root_mod helpers. |
| T04.2 | **Finite rings: fields & rings** — `finite_field_base`, `finite_field_constructor`, `finite_field_givaro`, `finite_field_ntl_gf2e`, `finite_field_pari_ffelt`, `finite_field_prime_modn`, `integer_mod_ring`. | 7 | T04.1 | ✅ Done | finite_field_base: FiniteField(Field) completed (53 methods; _roots_univariate_polynomial overloads); constructor: FiniteFieldFactory added; givaro: characteristic/degree corrected to Integer, Cache/modulus attrs typed; ntl_gf2e + pari_ffelt REWRITTEN (CI slop → source-grounded); prime_modn: hierarchy corrected to (FiniteField, IntegerModRing_generic); integer_mod_ring: IntegerModFactory added. Diamond alignments: FiniteField/IntegerModRing_generic shared-method signatures reconciled (characteristic/degree/modulus/random_element/gen/_magma_init_/_macaulay2_init_); QuotientRing_nc.construction -> tuple[Functor, Ring] | None, gen/gens -> Element; Rings.ParentMethods.gen -> Element; ConstructionFunctor(Functor); algebraic_closure_finite_field.pyi rewritten (CI slop → source hierarchy, L65/L536). |
| T04.3 | **Finite rings: homs & residue fields** — `hom_finite_field`, `hom_finite_field_givaro`, `hom_prime_finite_field`, `homset`, `galois_group`, `maps_finite_field`, `residue_field`, `residue_field_givaro`, `residue_field_ntl_gf2e`, `residue_field_pari_ffelt`, `conway_polynomials`, `algebraic_closure_finite_field`. | ~12 | T04.2 | ✅ Done | hom_finite_field: generic hom + Frobenius (26 methods) completed; givaro/prime variants; homset: 8 methods (__call__ inherited from Homset base — overload-set diamond); galois_group: GF element/group; maps_finite_field: vector-space isomorphisms; residue_field: factory + generic + ReductionMap + homomorphism_global + LiftingMap + prime_modn variant; givaro/ntl_gf2e/pari_ffelt residue variants; conway_polynomials: PseudoConwayLattice + 5 helpers; algebraic_closure_finite_field: full 3-class surface (element 21, generic 21, pseudo_conway). Zero Any/object in the set. |
| T04.4 | **Number field core** — `number_field`, `number_field_base`, `number_field_element`, `number_field_element_base`, `number_field_element_quadratic`, `number_field_ideal`, `number_field_ideal_rel`, `number_field_rel`, `number_field_morphisms`, `morphism`. | ~10 | — | ✅ Done | The complete named number-field core surface passes public-definition and public-method parity, structural checks, and the no-erasure semantic audit against the pinned Sage source. |
| T04.5 | **Number field auxiliaries** — `order`, `order_ideal`, `unit_group`, `class_group`, `galois_group`, `homset`, `maps`, `structure`, `selmer_group`, `bdd_height`, `S_unit_solver`, `splitting_field`, `small_primes_of_degree_one`. | ~13 | T04.4 | ✅ Done | The complete named number-field auxiliaries surface passes public-definition and public-method parity, structural checks, and the no-erasure semantic audit against the pinned Sage source. |
| T04.6 | **Number field: totally real / Kuni** — `totallyreal`, `totallyreal_data`, `totallyreal_rel`, `totallyreal_phc`. | 4 | T04.4 | ✅ Done | The complete named totally-real number fields surface passes public-definition and public-method parity, structural checks, and the no-erasure semantic audit against the pinned Sage source. |
| T04.7 | **p-adic generic infrastructure** — `padic_generic`, `padic_generic_element`, `padic_base_generic`, `padic_base_leaves`, `padic_extension_generic`, `padic_extension_leaves`, `local_generic`, `local_generic_element`, `generic_nodes`, `factory`, `misc`. | ~11 | — | ✅ Done | The complete named p-adic generic infrastructure surface passes public-definition and public-method parity, structural checks, and the no-erasure semantic audit against the pinned Sage source. |
| T04.8 | **p-adic element implementations** — `padic_capped_absolute_element`, `padic_capped_relative_element`, `padic_fixed_mod_element`, `padic_floating_point_element`, `padic_ext_element`, `padic_ZZ_pX_element`, `padic_ZZ_pX_CA_element`, `padic_ZZ_pX_CR_element`, `padic_ZZ_pX_FM_element`, `padic_relaxed_element`, `padic_lattice_element`. | ~11 | T04.7 | ✅ Done | The complete named p-adic element implementations surface passes public-definition and public-method parity, structural checks, and the no-erasure semantic audit against the pinned Sage source. |
| T04.9 | **p-adic q-adic flint variants** — `qadic_flint_CA`, `qadic_flint_CR`, `qadic_flint_FM`, `qadic_flint_FP`, `relative_extension_leaves`, `relative_ramified_CA`, `relative_ramified_CR`, `relative_ramified_FM`, `relative_ramified_FP`, `eisenstein_extension_generic`, `unramified_extension_generic`. | ~11 | T04.7 | ✅ Done | The complete named q-adic and ramified p-adic variants surface passes public-definition and public-method parity, structural checks, and the no-erasure semantic audit against the pinned Sage source. |
| T04.10 | **p-adic auxiliaries** — `padic_valuation`, `padic_printing`, `lattice_precision`, `precision_error`, `pow_computer`, `pow_computer_ext`, `pow_computer_flint`, `pow_computer_relative`, `witt_vector`, `witt_vector_ring`, `tutorial`, `morphism`, `common_conversion`. | ~13 | T04.7 | ✅ Done | The complete named p-adic auxiliaries surface passes public-definition and public-method parity, structural checks, and the no-erasure semantic audit against the pinned Sage source. |
| T04.11 | **Function field core** — `function_field`, `function_field_polymod`, `function_field_rational`, `element`, `element_polymod`, `element_rational`, `constructor`. | 7 | — | ✅ Done | The complete named function-field core surface passes public-definition and public-method parity, structural checks, and the no-erasure semantic audit against the pinned Sage source. |
| T04.12 | **Function field morphisms / orders / ideals / places** — `maps`, `order`, `order_basis`, `order_polymod`, `order_rational`, `ideal`, `ideal_polymod`, `ideal_rational`, `place`, `place_polymod`, `place_rational`, `extensions`, `divisor`. | ~13 | T04.11 | ✅ Done | The complete named function-field morphisms, orders, ideals, and places surface passes public-definition and public-method parity, structural checks, and the no-erasure semantic audit against the pinned Sage source. |
| T04.13 | **Function field differentials / derivations / valuation / Jacobian** — `differential`, `derivations`, `derivations_polymod`, `derivations_rational`, `valuation`, `valuation_ring`, `jacobian_base`, `jacobian_hess`, `jacobian_khuri_makdisi`, `jacobian_unique_hess`, `hermite_form_polynomial`, `riemann_roch`, `khuri_makdisi`. | ~13 | T04.11 | ✅ Done | The complete named function-field differentials, valuations, and Jacobians surface passes public-definition and public-method parity, structural checks, and the no-erasure semantic audit against the pinned Sage source. |
| T04.14 | **Function field: drinfeld_modules subpackage** — full subdir. | 7 + re-export shell | T04.11 | ✅ Done | Seven public-surface modules and the supporting `sage.categories.drinfeld_modules` contract were audited against the pinned Sage source. Constructor coefficient shapes, category-injected parent methods, finite-versus-lazy series return types, list-valued homset bases, Frobenius algorithm literals, and morphism/isogeny dependent returns are represented without `Any` or direct `object` returns. `all.py` is an exempt re-export shell under the feature scope policy. |
| T04.15 | **Valuation subpackage** — `valuation`, `augmented_valuation`, `developing_valuation`, `gauss_valuation`, `inductive_valuation`, `limit_valuation`, `mapped_valuation`, `scaled_valuation`, `trivial_valuation`, `valuation_space`, `valuations_catalog`, `value_group`. | ~12 | — | ✅ Done | The complete named valuation subpackage surface passes public-definition and public-method parity, structural checks, and the no-erasure semantic audit against the pinned Sage source. |
| T04.16 | **Asymptotic ring subpackage** — `asymptotic_ring`, `asymptotic_expansion_generators`, `asymptotics_multivariate_generating_functions`, `growth_group`, `growth_group_cartesian`, `term_monoid`, `misc`. | 7 | — | ✅ Done | Full expansion, growth-group, term-monoid, Cartesian-product, generating-function asymptotics, coefficient-map, localization, and construction-functor surfaces are source-grounded. |
| T04.17 | **Semirings** — full `sage.rings.semirings/` subpackage: nonnegative integers, tropical base semiring and coercions, tropical matrices, polynomial and multivariate-polynomial parents/elements, and tropical varieties. | 7 | — | ✅ Done | Source-grounded coefficient types, parent/element relations, coercion maps, matrix closures, polynomial constructors, Newton polytopes, dual subdivisions, and variety/plotting return types. |
| T04.18 | **Remaining Phase 04 root-level ring modules** — `bernmm` and any root-level `sage.rings` modules assigned by the Phase 01 inventory but not owned by Phases 02–03, including `noncommutative_ideals`, `species`, and `cfinite_sequence` where in scope. | — | 02, 03 | ⚪ N/A | Ownership reconciliation found no Phase 04 implementation work: root `bernmm` is T02.3; `noncommutative_ideals` is T02.5; `cfinite_sequence`, `species`, and the `bernmm` package surface are T02.10. Those completed Phase 02 tasks own the named modules. |

## Parallelism

- All four subtrees (finite_rings, number_field, padics, function_field)
  are mutually independent — assign one per agent.
- Within each subtree, the chain is core → element implementations →
  auxiliaries (see Depends column).

## Risks

- p-adic stubs touch C-level flint pow computers; signature drift between
  template `.pxi` files is common. Stick to the public `.pyx` surface.
- Number field stubs reference `sage.libs.pari` — those stubs come in
  Phase 18. Use string forward references where needed.
